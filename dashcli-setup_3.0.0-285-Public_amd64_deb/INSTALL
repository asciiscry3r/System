DASH-SDK-2.3

INSTALL instructions:
1. Create environment (install dependency packages)
	sh env.sh
2. Install dash-sdk package (need to logged in as a root or user with sudo privileges)
	i. for ubuntu debian package
	   dpkg -i <package-name>.deb
	ii. for fedora/opensuse rpm package
	   rpm -i <package-name>.rpm
3. help command:
	dashcli
